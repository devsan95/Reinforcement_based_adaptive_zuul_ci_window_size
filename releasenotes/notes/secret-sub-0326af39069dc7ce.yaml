---
upgrade:
  - |
    The ``sub`` claim in Zuul OIDC secrets was incorrectly constructed
    using the canonical project name of the project under test rather
    than the project where the secret was defined.  This has been
    corrected; third-party systems configured to accept OIDC secrets
    may need to have their authorization rules adjusted to accomodate
    the new values.
